# Inke3 全栈开发指挥手册

> **项目状态**：基础架构已跑通（Vite + Vercel + Supabase + Lightsail DNS）
> **核心原则**：先同步代码再推送，先说明联动再写功能。

---

## 快速导航

1. [填坑历史记录](#填坑历史记录)
2. [Git 同步急救包](#git-同步急救包)
3. [function模块待办](#function模块待办)
4. [核心联动包逻辑](#核心联动包逻辑)
5. [部署与协作规范](#部署与协作规范)

---

## 填坑历史记录

### 2026-01-29 | 域名与部署排坑
* **域名解析**：`inke3.com` 不生效。  
  **解决**：将 DNS 换成 Lightsail 提供的 **4 个 NS 地址**。
* **存储空间**：作品无法上传。  
  **解决**：在 Supabase 新建 `works` **Bucket** 并将 RLS 设为公开。
* **刷新 404**：子页面刷新报错。  
  **解决**：根目录建 `vercel.json` 配置 `rewrites` 重定向。
* **推送失败**：报错 `rejected`。  
  **解决**：执行 `git pull --rebase` 同步后再 push。

### 2026-01-28 | 接口与基础调优
* **API 适配**：AI 生成接口无效。  
  **解决**：手动更换为 Supabase 官网 URL/Key。
* **pnpm 报错**：插件安装失败。  
  **解决**：删掉 `pnpm-lock.yaml` 或切换为 `npm install`。
* **Auth 注册**：注册无反应。  
  **解决**：在 Supabase 后台开启 Authentication 注册开关。

---

### 2026-01-29 - 大功能更新：点赞收藏通知系统 + UI 升级

#### 🎉 新增功能

##### 1. 点赞和收藏系统
- ✅ 作品详情页添加点赞和收藏按钮
- ✅ 实时统计点赞数和收藏数
- ✅ 用户可取消点赞/收藏
- ✅ 防止重复点赞/收藏（数据库唯一约束）
- ✅ 触发器自动更新计数

**数据库变更：**
- 新增 `likes` 表
- 新增 `favorites` 表
- `works` 表添加 `likes_count` 和 `favorites_count` 字段

##### 2. 通知系统
- ✅ 完整的通知中心功能
- ✅ 导航栏显示未读通知数量（小红点）
- ✅ 支持 4 种通知类型：
  - 作品被点赞
  - 作品被收藏
  - 帖子被评论
  - 评论被回复
- ✅ 通知自动生成（数据库触发器）
- ✅ 标记已读/全部已读功能
- ✅ 点击通知跳转到相关内容

**数据库变更：**
- 新增 `notifications` 表
- 创建 4 个触发器自动发送通知

**实现细节：**
- 使用 PostgreSQL 触发器实现实时通知
- 每 30 秒自动刷新未读数量
- 最多显示最近 20 条通知

##### 3. 个人中心收藏列表
- ✅ 新增"收藏"标签页
- ✅ 展示用户收藏的所有作品
- ✅ 统计显示收藏数量
- ✅ 赛博朋克风格设计

#### 🎨 UI/UX 优化

##### 1. Logo 和品牌升级
- ✅ 设计全新锈银风格 logo
- ✅ 三个水滴元素 + 横版排版
- ✅ 透明背景，发光效果
- ✅ 更新导航栏和首页 logo
- ✅ 更新网站 favicon

##### 2. 显示逻辑优化
- ✅ 优先显示用户昵称（display_name）
- ✅ 昵称为空时降级显示用户名
- ✅ 全站统一显示逻辑
- ✅ 修复了作品卡片、作品详情、个人中心等多处显示

##### 3. 导航修复
- ✅ 修复 Hero 组件按钮使用 `<a>` 标签导致页面刷新的问题
- ✅ 改用 React Router 的 `<Link>` 组件
- ✅ 实现单页应用的流畅跳转

#### 🔧 技术改进

##### 数据库架构
```sql
-- 新增表
- likes (id, user_id, work_id, created_at)
- favorites (id, user_id, work_id, created_at)
- notifications (id, user_id, actor_id, type, work_id, post_id, comment_id, is_read, created_at)

-- 触发器
- update_likes_count() - 自动更新点赞数
- update_favorites_count() - 自动更新收藏数
- notify_work_like() - 点赞时发送通知
- notify_work_favorite() - 收藏时发送通知
- notify_post_comment() - 评论时发送通知
- notify_comment_reply() - 回复时发送通知
```

##### 组件架构
- 新增 `NotificationBell.tsx` 组件
- 优化 `WorkCard.tsx` 支持 authorDisplayName 参数
- 更新 `WorkDetail.tsx` 添加点赞收藏功能
- 更新 `Profile.tsx` 添加收藏列表

#### 📊 功能统计

**完成的核心功能：**
- [x] 用户系统（注册/登录/个人中心）
- [x] 作品发布和章节管理
- [x] 论坛系统（发帖/评论/楼中楼）
- [x] 点赞和收藏
- [x] 通知系统
- [x] 搜索功能
- [x] 标签系统

**本次新增：**
- 3 个新数据库表
- 6 个数据库触发器
- 1 个新组件
- 4 个功能模块

#### 🐛 已修复的问题
- ✅ Hero 页面按钮点击刷新页面（改用 Link）
- ✅ 作品卡片显示 username 而非 display_name
- ✅ WorkCard 组件缺少 authorDisplayName 参数
- ✅ Logo 排版问题（设计新的横版 logo）

#### 📝 开发笔记

#### 遇到的挑战
1. **中文文件名问题**：logo 文件名包含中文，需要确保路径正确
2. **Logo 设计迭代**：从原始竖版到横版，再到透明背景，经过多次调整
3. **Cursor 额度限制**：开发过程中 Cursor 用量达到上限，转为手动编码

##### 最佳实践
1. **数据库触发器**：使用触发器自动维护计数和发送通知，减少应用层逻辑
2. **组件参数设计**：WorkCard 同时接收 author 和 authorDisplayName，提供灵活的降级显示
3. **通知轮询**：30秒轮询未读数，平衡实时性和性能

#### 🎯 下一步计划

**短期（本周）：**
1. 移动端适配测试和优化
2. 添加 Toast 提示（成功/失败反馈）
3. 设计 404 页面
4. 全面测试现有功能

**中期（下周）：**
1. 热门作品排行榜
2. 搜索功能增强（历史记录、高级筛选）
3. 图片懒加载优化
4. 分页功能

**长期：**
1. 用户关注系统
2. 私信功能
3. 内容推荐算法
4. 数据统计面板

#### 💻 技术栈确认
- **前端**：React 18 + TypeScript + Vite + Tailwind CSS
- **路由**：React Router v6
- **UI 组件**：Radix UI + shadcn/ui
- **后端**：Supabase (PostgreSQL + Auth + Storage)
- **部署**：Vercel
- **版本控制**：Git + GitHub

#### 📈 项目进度
- 核心功能完成度：**85%**
- UI/UX 完成度：**80%**
- 移动端适配：**60%**
- 性能优化：**70%**

---

**开发时长**：约 4-5 小时  
**提交者**：与 Claude (Anthropic) 协作开发  
**Git 分支**：feat/nested-replies → main

---

### 2026-01-30 | 手机端页面修复
* **手机端**：解决了手机端网页不适配问题  
  **解决**：增添首页汉堡栏

### 2026-01-31 待解决：
- 作品页面：在个人中心的作品页面进行编辑功能，增添章节，批量增添章节，编辑章节文案标题标签
- 作品页面：增添评论，下一章，章节评论功能
- 首页作品封面大小改小，作品页作品封面改小
- 闲聊版面改个名字
- 楼中楼回复增加到三级可同楼回复
- 论坛置顶功能

## Git 同步急救包

*注意：每次推送代码前，务必按照以下顺序操作，防止冲突*

```bash
# 1. 基础上传流程
git add . 
git commit -m "feat: 这里写你改了什么"
git push origin main

# 2. 推送失败修复
git pull origin main --rebase

# 3. 身份配置
git config user.email "你的邮箱"
git config user.name "你的名字"

---

# 1. 创建并切换到一个新分支 (比如叫 dev-comment)
git checkout -b dev-comment

# 2. 在这个分支上随便改代码，然后正常提交
git add .
git commit -m "feat: 尝试添加评论功能"

# 3. 如果写好了，想合并回主线
git checkout main       # 先回到主线
git merge dev-comment   # 把刚才写的评论功能合进来

---
### 🌿 开发分支流 (Feature Branch Workflow)
1. **开工**：`git checkout -b 分支名` (例如 `git checkout -b feat-join-btn`)
2. **提交**：`git add .` -> `git commit -m "完成某功能"`
3. **回主线**：`git checkout main`
4. **拿最新**：`git pull origin main` (防止别人改了代码)
5. **合代码**：`git merge 分支名`
6. **推线上**：`git push origin main`

```

---

## function模块待办

按板块划分，确保 UI 统一与数据联动。

### 1. 首页 (Home)
- [ ] **功能**：增加“加入我们”快捷按钮。
- [ ] **联动**：点击后判断登录状态，未登录跳转注册，已登录跳转作品库。

### 2. 作品库 (Works Library)
- [ ] **功能**：瀑布流展示作品，支持点击进入详情。
- [ ] **联动**：作品卡片 (`WorkCard`) 上的点赞数需从 `likes` 表实时读取。

### 3. 论坛板块 (Forum)
- [ ] **功能**：完善发帖表单（支持 Markdown 或富文本）。
- [ ] **联动**：增加评论回复功能；帖子分类（技术交流、作品反馈）。

### 4. 个人中心 (Profile)
- [ ] **功能**：展示“我的作品”与“我的收藏”。
- [ ] **联动**：当用户在作品库点击收藏，此处列表需同步增加。

---

## 核心联动包逻辑

> **提示**：开发新功能前，先将对应的联动逻辑发给 AI，确保全站风格与数据同步。

| 联动名称 | 出发点 (UI) | 存储点 (DB) | 同步影响 (Impact) |
| :--- | :--- | :--- | :--- |
| **身份权限** | 登录/退出按钮 | `auth.users` | 只有作者能编辑作品，未登录隐藏功能。 |
| **点赞热度** | 卡片爱心图标 | `likes` 表 | 图标变色，数字 +1，个人中心显示被赞总数。 |
| **评论社交** | 作品评论框 | `comments` 表 | 评论列表实时刷新，作者收到红点通知。 |
| **个人资料** | 设置页修改头像 | `profiles` 表 | 论坛、作品、评论区头像全站实时同步。 |

---

## 部署与协作规范

* **指挥 AI 规范**：先喂本日志，再给现有组件代码，最后下达“联动指令”。
* **Git 规范**：`pull --rebase` 同步后再 `push`。
* **测试规范**：Vercel 部署完成后，必须使用浏览器 **无痕模式** 访问测试。

### 长期待办
- [ ] 备份 Supabase 数据库表结构（SQL 脚本）。
- [ ] 接入评论系统（联动：作品详情页 <-> 个人中心消息提醒）。